---
presets:
- name: futures_all
  filter:
  - left: typespecs
    operation: has
    right: continuous
  - left: sector
    operation: nempty
  sort:
    sortBy: name
    sortOrder: asc
- name: futures_agricultural
  filter:
  - left: typespecs
    operation: has
    right: continuous
  - left: sector
    operation: equal
    right: Agricultural
  sort:
    sortBy: name
    sortOrder: asc
- name: futures_energy
  filter:
  - left: typespecs
    operation: has
    right: continuous
  - left: sector
    operation: equal
    right: Energy
  sort:
    sortBy: name
    sortOrder: asc
- name: futures_currencies
  filter:
  - left: typespecs
    operation: has
    right: continuous
  - left: sector
    operation: equal
    right: Currencies
  sort:
    sortBy: name
    sortOrder: asc
- name: futures_metals
  filter:
  - left: typespecs
    operation: has
    right: continuous
  - left: sector
    operation: equal
    right: Metals
  sort:
    sortBy: name
    sortOrder: asc
- name: futures_world_indices
  filter:
  - left: typespecs
    operation: has
    right: continuous
  - left: sector
    operation: equal
    right: Indexes
  sort:
    sortBy: name
    sortOrder: asc
- name: futures_interest_rates
  filter:
  - left: typespecs
    operation: has
    right: continuous
  - left: sector
    operation: equal
    right: Financials
  sort:
    sortBy: name
    sortOrder: asc
- name: country_page_futures_commodities_ar
  symbols:
    tickers:
    - "MATBAROFEX:SOY.CME1!"
